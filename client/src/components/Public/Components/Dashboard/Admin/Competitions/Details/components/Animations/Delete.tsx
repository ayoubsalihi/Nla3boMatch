import Lottie from "lottie-react"
import DeleteIcon from "../Animations/Json/Delete.json"


const Delete = () => {
  return (
    <div>
        <Lottie animationData={DeleteIcon} loop={true}/>
    </div>
  )
}

export default Delete